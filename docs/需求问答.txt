1.v1 你要的是“第一次提交就覆盖全部功能”，还是“内部允许分里程碑推进，但最终目标必须全覆盖”？
我默认：内部按阶段推进，但目标始终是全功能迁移。

2.新版 UI 你希望“尽量复刻现在的布局和交互”，还是“允许我重构界面，只保留功能等价”？
我默认：保留现有工作流和页面结构，但允许我把界面做得更稳、更清晰。
另外，如果你没特别要求，我会选 Qt Widgets，不选 QML。

3.现有 scripts/*.py 你希望怎么处理？
是要新版继续兼容这些 Python 脚本，还是要求它们也逐步改写成 C++ 原生任务流？

4.glasses_bsp_node / xrglasses 这块，你手里有没有 Linux 下可直接用于 C++ 的 SDK、头文件、动态库、示例代码或文档？
如果有，请告诉我它们现在大概在哪里；如果没有，我会把这块当成最高风险项单独规划。

5.新版是否必须与现有系统保持严格兼容：
包括 agents_config.json、topic 名、端口、消息格式、录制目录结构、CSV 字段、图片命名、record_info.txt 等。
我默认：严格兼容，方便对照验证，也方便新旧混跑。

6.新工程里是否允许我直接复用或迁入仓库中现有的 C++ echo_message_system 实现，只要代码最终放在 RecordLabC 里即可？
还是你要我只把它当参考，重新写一套自己的 IPC？

7.依赖边界你有什么限制吗？
例如 Qt6、ZeroMQ、nlohmann/json、libssh、OpenCV、ALSA、FFmpeg 这类常见 C++ 依赖，是否都可以纳入设计，只要后续由你自己决定装不装环境？

8.开发策略上，你是否允许我在早期版本里做“过渡兼容层”？
比如某些最难的设备/脚本能力先保接口兼容，再逐步替换成纯 C++ 实现。
还是你要求每个做出来的模块从第一天起就必须完全原生 C++/Qt？

9.你最在意的优先级是什么？
如果只能先把一条链路做到最稳，你希望我优先打通哪一条：imu_simulation、glasses_bsp_node、glasses_nviz_node、UR_node、还是脚本系统/UI 控制中心？

1.按你默认 2.保留现有工作流和页面结构，但允许我把界面做得更稳、更清晰。我不明白c++qt和pyqt的性能的有什么区别，之前版本为了规避python的u全局解释锁做了一些努力。3.脚本更像“编排层”或“运维层”，不是核心高频链路。像 record_bsp_imu_cam.py 这种主要是在组织录制流程、调 agent.cmd(...)、打印状态；check_environment.py 是环境诊断；play_video_on_secondary_screen.sh 这类就是本地 shell 封装。这些改成 C++ 的收益很低，维护成本反而更高。一般python脚本容易维护，不需要改成c++ ，与项目的c++语言完美兼容就行。 4.RecordLab/xreal_glasses-0.3.3-py3-none-any.whl和RecordLab/XrGlasses.pyi是你需求的东西吗，我不太懂 5.按你默认，必须一致。6.可以完全复用 7没有限制 8.要求每个做出来的模块从第一天起就必须完全原生 C++/Qt脚本不需要） 9.先打通glasses_bsp_node相关的所有东西，这是核心，具体你可以看RecordLab/docs/RecordLab录数据使用指南.md